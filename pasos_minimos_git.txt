Pasos ordenados para inicializar la subida por git:

1.Abrir terminal
-comandos:
    git init
    
    git add .    <-- AÑADE todos los archivos (por el .)

    git commit -m "Etc..."

	-> CREAR REPOSITORIO EN GITHUB (WEB) - nos facilitará el código al final de la página

    $ git remote add origin https://github.com/Noteverlasting/resumen01.git (por ejemplo)
    git branch -M main


    git push -u origin main